document.addEventListener('DOMContentLoaded', function() {
    // Fetch and display overview statistics
    fetchOverviewStats();
    
    // Initialize charts
    initCharts();
});

function fetchOverviewStats() {
    // Fetch data from backend and update the overview-stats element
}

function initCharts() {
    // Initialize and render various charts
}

// Add more functions for interactivity and data updates